server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/MyDatabase
    username: postgres
    password: myforpgdbpar

  jpa:
    hibernate:
      ddl-auto: update  # Hibernate сам создаст/обновит таблицы
    show-sql: true      # Печатает SQL в консоль
    properties:
      hibernate:
        format_sql: true
    open-in-view: false  # Убирает warn при запуске

#TODO сделать swagger